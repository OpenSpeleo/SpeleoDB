# Include geometry in account exports

When adding a user-owned dataset, audit account exports alongside its CRUD and
viewer flows. Database-backed data needs a portable export just as uploaded
files do. Reuse the canonical permission queryset and capture coordinates and
revision together. Use the requested archive names `geometries/` and `layers/`,
not model module names, and keep manifests, README, UI, and tests consistent.
