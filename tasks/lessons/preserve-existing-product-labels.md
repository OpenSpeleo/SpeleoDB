# Preserve Existing Product Labels

Adding sharing or broadening the data shown on a page does not authorize
renaming established product navigation or headings. Keep existing labels such
as "My GPS Tracks" unless the user explicitly requests copy changes, even when
the underlying list now includes shared entities.

Tests for new functionality should pin the established wording rather than
assuming the architecture change requires a terminology change.
