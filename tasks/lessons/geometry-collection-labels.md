# Use plural labels for geometry collections

The map card and management list represent a collection, so their titles,
loading/empty/error states, tooltips, and accessible labels say GIS Geometries.
Keep GIS Geometry for individual record settings and actions. Audit related
labels together rather than changing only the visible heading.
